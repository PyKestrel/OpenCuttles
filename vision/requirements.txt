# NOTE: torch/torchvision must be the CPU builds from the PyTorch CPU index, or
# transformers/timm will pull the CUDA build (which fails to import on a CPU-only
# host). Use install.sh, or:
#   pip install torch==2.5.1+cpu torchvision==0.20.1+cpu \
#     -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
transformers>=4.44,<4.52
pillow>=10.0.0
einops>=0.8.0
timm>=1.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
